# Project Technical Stack: blackjax-grooming

This document provides an overview of the technical stack, languages, and project structure for the `blackjax-grooming` project (identified as `house-of-puppies` in `package.json`).

## Project Overview
A modern web application built with **Next.js**, featuring authentication, responsive design, and database integration.

## Core Technology Stack
- **Framework**: [Next.js](https://nextjs.org/) (React 19)
- **Database / ORM**: [Prisma](https://www.prisma.io/) with **SQLite**
- **Authentication**: [NextAuth.js](https://next-auth.js.org/)
- **UI & Components**: [Radix UI](https://www.radix-ui.com/), [Lucide React](https://lucide.dev/), and [Shadcn UI](https://ui.shadcn.com/)
- **Styling**: [Tailwind CSS](https://tailwindcss.com/)

## Languages Used
- **TypeScript (`.ts`, `.tsx`)**: Primary language for application logic, API routes, and UI components.
- **JavaScript (`.mjs`)**: Used for configuration files (ESLint, PostCSS).
- **Prisma Schema Language (`.prisma`)**: Used for database modeling.
- **CSS**: Tailwind CSS for utility-first styling.
- **SQL**: Database operations (SQLite).
- **JSON**: Configuration (`package.json`, `tsconfig.json`).
- **Markdown**: Documentation (`README.md`).

## Project Structure
- **`/src/app`**: Contains the Next.js App Router folders, layouts, and pages.
    - `(dashboard)` / `(website)`: Route groups for different parts of the site.
    - `api`: Backend API endpoints.
- **`/src/components`**: Reusable UI components (buttons, cards, etc.).
- **`/src/lib`**: Utility functions, shared logic, and database client initialization.
- **`/prisma`**: Contains the `schema.prisma` file and the SQLite database (`dev.db`).
- **`/public`**: Static assets like images and icons.

---
*Generated by Antigravity AI on 2026-03-22*
